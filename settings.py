# Network
HOST_ADDR = ""        # Leave as empty string "" if client is on the same computer as server. Else, input host computers ip, e.g. "192.168.10.245" (shown in server window)
HOST_PORT = 8090

# Game
start_pos = (20, 20)  # Where the player will spawn